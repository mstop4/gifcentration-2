import React, { ReactElement } from 'react';
import styles from '@/styles/elements/ui/Spinner.module.scss';

export default function Spinner(): ReactElement {
  return <div className={styles.spinner}></div>;
}
